#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t = 1;
    while (t != 42)
    {
        int x;
        cin >> x;
        if (x != 42)
            cout << x << endl;
        t = x;
    }
    return 0;
}