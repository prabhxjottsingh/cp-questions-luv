#include <bits/stdc++.h>
using namespace std;

int main()
{

    //to divide a number with two, rigth shift it by one place
    cout << (5 >> 1) << endl;

    //multiply a number with 2, using right shift
    cout << (5 << 1);
}